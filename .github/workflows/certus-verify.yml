name: Certus Verify

on:
  workflow_dispatch:
  schedule:
    - cron: "0 3 * * *"  # 03:00 UTC daily

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install deps
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Create .env if missing (no secrets)
        run: |
          if [ ! -f .env ]; then
            echo "CERTUS_PAUSED=false" > .env
          fi

      - name: Run verification
        # Run the CI-friendly API checker which writes results/api_check.json
        run: python -u scripts/action_check_apis.py
